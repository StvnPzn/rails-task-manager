<h1>Tasks List</h1>
<ol>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task.id) %> (<%= task.completed %>)</li>
  <% end %>
</ol>

<h2><%= link_to 'Create a new task', new_task_path %></h2>
